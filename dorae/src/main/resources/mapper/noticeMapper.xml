<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="notice">
	
	<insert id="create">
		insert into notice values (#{notice_id}, #{admin_id}, #{title}, #{content}, #{created_at}, #{tag})
	</insert>
	
	<select id="selectOne" resultType="noticeVO">
		select * from notice where notice_id = #{notice_id}
	</select>
	
	<select id="selectList" resultType="noticeVO">
		select * from notice
	</select>
	
	<update id="update" parameterType="noticeVO">
		update notice set title = #{title}, content = #{content}, tag = #{tag} where notice_id = #{notice_id}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from notice where notice_id = #{notice_id}
	</delete>
	
</mapper>
